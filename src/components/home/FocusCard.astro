---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="bg-white p-6 rounded-lg shadow-md">
    <h3 class="text-xl font-semibold mb-3 text-primary-600">{title}</h3>
    <p><slot /></p>
</div>
